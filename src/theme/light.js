import { createTheme } from '@mui/material';

const lightTheme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#000000', // Replace with your desired black color code
    },
    // Add other light theme customizations here
  },
});

export default lightTheme;
